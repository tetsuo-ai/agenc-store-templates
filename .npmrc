# Install optional dependencies (the platform-specific @next/swc-* native
# binaries are next's optionalDependencies; some npm/workspace combinations skip
# them otherwise, which breaks `next build` with "Failed to load SWC binary").
optional=true
include=optional
